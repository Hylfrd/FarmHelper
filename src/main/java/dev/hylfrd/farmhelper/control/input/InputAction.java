package dev.hylfrd.farmhelper.control.input;

/** Every holdable input that FarmHelper may manage. */
public enum InputAction {
    FORWARD,
    BACKWARD,
    LEFT,
    RIGHT,
    JUMP,
    SNEAK,
    SPRINT,
    ATTACK,
    USE
}
