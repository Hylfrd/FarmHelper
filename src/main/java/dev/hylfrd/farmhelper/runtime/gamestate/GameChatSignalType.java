package dev.hylfrd.farmhelper.runtime.gamestate;

/** Shared, identity-free meanings recognized from system chat. */
public enum GameChatSignalType {
    LIMBO_ENTERED,
    GUEST_ARRIVED,
    PEST_SPAWNED,
    REPELLENT_ACTIVATED,
    EVACUATION_REQUIRED
}
