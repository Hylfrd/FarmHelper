package dev.hylfrd.farmhelper.ui.settings;

/** Reusable value shapes supported by the project-owned settings UI. */
public enum SettingKind {
    BOOLEAN,
    INTEGER,
    DECIMAL,
    ENUM,
    STRING,
    COLOR,
    KEYBIND,
    ACTION
}
