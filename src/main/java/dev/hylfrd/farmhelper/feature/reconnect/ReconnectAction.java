package dev.hylfrd.farmhelper.feature.reconnect;

/** Semantic client action requested through a reconnect port. */
public enum ReconnectAction {
    NONE,
    DISCONNECT,
    CONNECT,
    ENTER_SKYBLOCK,
    RETURN_TO_LOBBY,
    WARP_GARDEN,
    SHOW_TITLE_SCREEN
}
