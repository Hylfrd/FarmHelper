package dev.hylfrd.farmhelper.navigation.simulation;

/** Outcome of one bounded zero-input stopping prediction. */
public enum FlyStoppingPredictionOutcome {
    STOPPED,
    TICK_LIMIT,
    UNKNOWN
}
